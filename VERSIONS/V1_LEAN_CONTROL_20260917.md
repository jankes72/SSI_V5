# SSI V5 — V1 Lean Control / Reference

**Published:** 2026-09-17  
**Role:** lean S10 control/reference line  
**Public boundary:** evidence and architecture description only; proprietary implementation remains private.

## Identity

V1 is the deliberately lean Router S10 comparison line.

```text
V1 = lean control/reference
execution path = FAST_PASS -> AUTO_EVALUATE_DEEP
```

V1 is intended to expose the direct S10 evaluation path with minimal surrounding optimization infrastructure.

## Preserved core mechanisms

- Hermes competence memory;
- FAST_PASS family grouping;
- deep comparison;
- evidence-backed Champion / Challenger proposals;
- consolidation planning;
- versioned descendant/upgrade workflow;
- calibrated S10 thresholds;
- negative-memory preservation;
- immutable FAIR baseline enforcement;
- PRE_S10 integrity gate;
- audit/evidence history.

## Intentionally absent from V1

V1 does not use the later optimization layers that distinguish the other comparison lines:

```text
NO V2 INCREMENTAL_PARTIAL
NO V2 META_CACHE_REUSE
NO V3 MetaNetwork layer
NO V4 State-Space / Diana / Recursio bridge
```

## Historical retained V1 evidence snapshot

The repository already preserves a scoped V1 snapshot with:

```text
MODE = AUTO_EVALUATE_DEEP
SOURCE_BODIES = ISKRA4 + ISKRA5
SKILL_VARIANTS = 144
S10_FAMILIES = 13
DEEP_COMPARE_COMPLETED = 13/13
DEEP_PAIR_COUNT = 1890
ACTIVE_PROFILE = GRID_SAFETY_MAX
PRE_S10_GATE = PASS
IMMUTABLE_ENFORCEMENT = TRUE
REGRESSION_FOUND = FALSE
```

This historical snapshot is a two-BODY control snapshot, not a seven-BODY benchmark.

## Current comparison role

V1 is retained as the transparent recomputation/reference line for matched S1-S10 experiments against V2, V3 and V4.

The numbering is lineage, not a performance ranking.
